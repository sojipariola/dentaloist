# Deployment (Extended)

- Docker compose for backend and Postgres.
- Frontend runs separately with `npm start` (in development).
- Production: build frontend and serve via CDN or behind NGINX.
- Configure Stripe keys in environment.
